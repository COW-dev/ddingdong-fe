import { Loading } from '@/components/loading/Loading';

export default function RankingLoading() {
  return (
    <div className="flex w-full justify-center py-[3.2rem] md:py-[4.8rem]">
      <Loading />
    </div>
  );
}
